server:
  port: 8090
spring:
  application:
    name: test
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:8081/eureka/,http://admin:admin123@localhost:8082/eureka/
    fetch-registry: true
    register-with-eureka: true